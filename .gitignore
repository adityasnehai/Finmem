# Environment — never commit keys
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# Next.js
web/.next/
web/node_modules/
web/out/

# Test / notebook artifacts
results/*.json
notebooks/.ipynb_checkpoints/
*.png
*.pdf

# finmem_db/ — intentionally tracked (committed after ingest)
# hmm_regime.pkl — intentionally tracked (committed after ingest)

# Market data cache — generated at runtime, not committed
data/market_cache.parquet
